## Summary

## Validation

- [ ] `go build ./...`, `go test ./...`, `gofmt -l .`, `go vet ./...` pass
- [ ] `make build-workflow` still produces a working `.alfredworkflow`, if `workflow/info.plist` changed
- [ ] No secrets or credentials included
